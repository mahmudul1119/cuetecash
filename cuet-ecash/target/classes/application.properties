# JWT Configuration
jwt.secret=your_super_secret_key_that_is_at_least_256_bits_long

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/cuet_ecash?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=ZaYaD2017
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
# Update existing tables, don't recreate them
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Disable automatic data loading to avoid conflicts
spring.sql.init.mode=never
server.port=5454

# Enable debug logging for authentication
logging.level.org.springframework.security=DEBUG
logging.level.com.cuetecash=DEBUG
logging.level.org.springframework.web=DEBUG

